rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /applications/{id} {
      allow create: if true;
      allow read, update, delete: if false;   // protects minor PII
    }
    match /meta/waitlist {
      allow read, write: if true;             // just an integer counter
    }
  }
}